apiVersion: v1
kind: Service
metadata:
  name: multi-container-nodeport
spec:
  type: NodePort
  selector:
    app: multi-container
  ports:
  - name: nginx
    port: 80
    targetPort: 80
    protocol: TCP
    nodePort: 30080
